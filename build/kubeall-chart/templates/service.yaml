apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeall-chart.fullname" . }}
  labels:
    {{- include "kubeall-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubeall-chart.selectorLabels" . | nindent 4 }}
